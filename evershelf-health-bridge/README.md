# EverShelf Health Bridge — Android gateway for Fuel Mode
#
# Pairing: scan the QR from EverShelf Settings → Health (URL + token).
# Build: GitHub Actions only (`.github/workflows/build-health-bridge.yml`).
# Download: https://github.com/dadaloop82/EverShelf/releases/download/health-bridge-latest/evershelf-health-bridge.apk

Do not build on the EverShelf server — use GitHub Actions / workflow_dispatch.
